{#
    <EmptyState> OOPS : illustration document + loupe (croppée maquette),
    titre, message et bouton outline violet vers le listing d'activités.
    Variables : message (HTML autorisé pour les retours à la ligne).
#}
<div class="acc-empty">
    <img class="acc-empty__img" src="{{ asset('images/account/empty-oops.jpg') }}" alt="">
    <p class="acc-empty__title">OOPS</p>
    <p class="acc-empty__text">{{ message|raw }}</p>
    <a href="{{ path('app_activities') }}" class="pl-btn pl-btn--outline-action">Parcourir les activités</a>
</div>

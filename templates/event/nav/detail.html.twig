{% extends 'base.html.twig' %}
{% import '_partials/_icons.html.twig' as icon %}

{% block title %}Compétitions Canoë / Kayak{% endblock %}
{% block meta_description %}Compétitions Canoë / Kayak à Autrans : rejoignez l'événement, découvrez les participants et réservez votre place.{% endblock %}

{% block header %}
    {% include '_partials/navbar_events.html.twig' with { connected: true, nav_active: 'evenements' } %}
{% endblock %}

{% block body %}
<div class="evn-page evn-detail">
    <div class="home-container">

        <nav class="act-breadcrumb" aria-label="Fil d'Ariane">
            <a href="{{ path('app_home') }}">Accueil</a>
            <span aria-hidden="true">/</span>
            <a href="{{ path('app_events') }}">Événements</a>
            <span aria-hidden="true">/</span>
            <span aria-current="page" class="evn-crumb-current">Compétitions Canoë / Kayak</span>
        </nav>

        {% include 'event/nav/_banner.html.twig' with {
            text: "Devenez organisateur : créez un événement aujourd'hui !",
            cta: 'Créer un événement',
        } %}

        <div class="evn-detail__layout">

            {# ========================================================
               COLONNE GAUCHE : titre, organisateur, groupe associé,
               détails, participants, photos, signalement, similaires.
               Typo corrigée : « Organiser par » → Organisé par.
               ======================================================== #}
            <div class="evn-detail__main">
                <h1 class="evn-detail__title">Compétitions Canoë / Kayak</h1>

                <p class="evn-organizer">
                    <img src="{{ asset('images/events/avatar-thomas.jpg') }}" alt="">
                    Organisé par <strong>Martin Thomas</strong>
                </p>

                <a href="{{ path('app_group_detail') }}" class="evn-groupcard">
                    <img src="{{ asset('images/events/group-canoe-thumb.jpg') }}" alt="">
                    <span class="evn-groupcard__body">
                        <span class="evn-groupcard__name">Les passionnés de canoe -Kayak {{ icon.chevron_right(16) }}</span>
                        <span class="evn-groupcard__rating">
                            <span class="evn-stars" aria-label="4,5 étoiles sur 5">
                                {% for i in 1..5 %}<span class="{{ i <= 3 ? 'is-on' : 'is-off' }}">{{ icon.star(16) }}</span>{% endfor %}
                            </span>
                            <strong>4.5</strong>
                            <span class="evn-dot" aria-hidden="true"></span>
                            <a href="#">14 évaluations</a>
                        </span>
                    </span>
                </a>

                <h2 class="evn-h2">Détails</h2>
                <p class="evn-detail__text">Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.</p>

                <div class="evn-parts__head">
                    <h2 class="evn-h2">Participants <span class="evn-count-chip">12</span></h2>
                    <a href="{{ path('app_events_participants') }}" class="evn-btn-dark">Tout voir {{ icon.arrow_up_right(15) }}</a>
                </div>
                <div class="evn-parts">
                    {% for p in participants[1:4] %}
                        <div class="evn-part">
                            <span class="evn-part__avatar">
                                <img src="{{ asset(p.avatar) }}" alt="">
                                {% if loop.first %}<span class="evn-part__tag">Hôte</span>{% endif %}
                            </span>
                            <span class="evn-part__name">{{ p.name }}</span>
                            <span class="evn-part__role">{{ loop.first ? 'Assistant organisateur' : 'Membre' }}</span>
                        </div>
                    {% endfor %}
                    <a href="{{ path('app_events_participants') }}" class="evn-part evn-part--more">
                        <span class="evn-part__cluster">
                            {% for a in avatars[:3] %}<img src="{{ asset(a) }}" alt="">{% endfor %}
                            <span class="evn-part__plus">+8</span>
                        </span>
                        <span class="evn-part__name">+8 de plus</span>
                    </a>
                </div>

                <h2 class="evn-h2">Photos <span class="evn-count-chip">01</span></h2>
                <div class="evn-photos">
                    <img src="{{ asset('images/events/ev-photo1.jpg') }}" alt="Photo de l'événement">
                </div>

                <button type="button" class="evn-report-btn" id="evn-report-open">{{ icon.send(16) }} Signaler l'événement</button>
            </div>

            {# ========================================================
               COLONNE DROITE (sticky) : cover + flyer, réservation,
               « C'est pour offrir ? », agenda.
               ======================================================== #}
            <aside class="evn-detail__side">
                <div class="evn-cover">
                    <img src="{{ asset('images/events/ev-raft.jpg') }}" alt="">
                    <button type="button" class="evn-cover__flyer">{{ icon.image(16) }} Partager un flyer</button>
                </div>

                <div class="evn-book">
                    <p class="evn-book__price"><strong>À partir de 29€</strong> / personne</p>
                    <div class="evn-book__sep" aria-hidden="true"></div>
                    <p class="evn-book__label">Voyageurs</p>
                    <div class="evn-book__row">
                        <span>Adulte (18 et + )</span>
                        <span class="evn-stepper" data-evn-stepper data-min="1">
                            <button type="button" data-step="-1" aria-label="Moins d'adultes">{{ icon.minus(14) }}</button>
                            <output>2</output>
                            <button type="button" data-step="1" aria-label="Plus d'adultes">{{ icon.plus(14) }}</button>
                        </span>
                    </div>
                    <div class="evn-book__row">
                        <span>Enfants (0 à 17 ans )</span>
                        <span class="evn-stepper" data-evn-stepper data-min="0">
                            <button type="button" data-step="-1" aria-label="Moins d'enfants">{{ icon.minus(14) }}</button>
                            <output>0</output>
                            <button type="button" data-step="1" aria-label="Plus d'enfants">{{ icon.plus(14) }}</button>
                        </span>
                    </div>
                    <div class="evn-book__total">
                        <span>Total</span>
                        <strong>29€</strong>
                    </div>
                    <button type="button" class="pl-btn pl-btn--primary pl-btn--block">Réservez maintenant</button>
                </div>

                <div class="evn-gift">
                    <p class="evn-gift__title">C'est pour offrir ?</p>
                    <p class="evn-gift__text">Choisissez le bon cadeau  <strong>Plaisirs &amp; Loisirs</strong> et offrez cette expérience en quelques minutes !</p>
                    <a href="{{ path('app_gifts') }}" class="pl-btn pl-btn--primary pl-btn--block">Offrir maintenant</a>
                </div>

                <div class="evn-agenda">
                    <p class="evn-agenda__title">Ajouter à votre agenda {{ icon.calendar(18) }}</p>
                    <p class="evn-agenda__date">Lundi 25 Mai 2023 11h00 - 13h30</p>
                </div>
            </aside>
        </div>

        {# « Vous aimerez peut-être aussi » : cartes de type groupe
           (incohérence maquette assumée — point à trancher n°1). #}
        <section class="evn-section evn-section--similar">
            <h2 class="evn-section__title">Vous aimerez peut-être aussi</h2>
            <div class="card-grid evn-similar-grid">
                {% for g in similar %}
                    {% include 'event/nav/_group_card.html.twig' with { g: g, avatars: avatars } %}
                {% endfor %}
            </div>
        </section>

    </div>

    {% include 'event/nav/_report_modal.html.twig' %}
</div>
{% endblock %}
